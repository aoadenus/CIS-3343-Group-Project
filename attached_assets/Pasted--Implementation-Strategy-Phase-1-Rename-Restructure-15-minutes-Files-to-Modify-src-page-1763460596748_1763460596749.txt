🎯 Implementation Strategy
Phase 1: Rename & Restructure (15 minutes)
Files to Modify:

src/pages/Shop.tsx → Rename to src/pages/Menu.tsx

src/App.tsx → Update route from /shop to /menu

Navigation components → Update all "Shop" links to "Menu"

Actions:

bash
# In Replit Shell
cd src/pages
mv Shop.tsx Menu.tsx
Update Route in src/App.tsx:

tsx
// Change this:
<Route path="/shop" element={<Shop />} />

// To this:
<Route path="/menu" element={<Menu />} />
Phase 2: Reorganize Menu.tsx Content (30 minutes)
New Structure:

tsx
// src/pages/Menu.tsx
import { standardCakes, cakeSizes, flavors, fillings, icings, icingColors, decorations } from '@/data/cakeOptions';

export default function Menu() {
  return (
    <div className="min-h-screen bg-vanilla">
      {/* Hero Section */}
      <MenuHero />
      
      {/* Section 1: Standard Cakes */}
      <StandardCakesSection cakes={standardCakes} />
      
      {/* Section 2: Flavors, Fillings & Icings */}
      <CustomizationOptionsSection 
        flavors={flavors}
        fillings={fillings}
        icings={icings}
      />
      
      {/* Section 3: Icing Colors Palette */}
      <IcingColorsSection colors={icingColors} />
      
      {/* Section 4: Decorations Gallery */}
      <DecorationsSection decorations={decorations} />
      
      {/* Call to Action */}
      <ContactCTA />
    </div>
  );
}
Phase 3: Build Each Section Component
A. Hero Section
tsx
function MenuHero() {
  return (
    <section className="bg-raspberry-pink text-white py-20 text-center">
      <div className="container mx-auto px-4">
        <h1 className="text-5xl md:text-6xl font-bold mb-4">
          Our Menu
        </h1>
        <p className="text-xl md:text-2xl mb-8">
          Custom European-Style Cakes Made with Love
        </p>
        <div className="flex flex-col sm:flex-row gap-4 justify-center">
          <a 
            href="tel:713-555-2253" 
            className="bg-white text-raspberry-pink px-8 py-4 rounded-lg font-semibold hover:bg-cream transition"
          >
            📞 Call (713) 555-CAKE
          </a>
          <a 
            href="mailto:orders@emilybakescakes.com"
            className="bg-transparent border-2 border-white px-8 py-4 rounded-lg font-semibold hover:bg-white hover:text-raspberry-pink transition"
          >
            📧 Email Us
          </a>
        </div>
      </div>
    </section>
  );
}
B. Standard Cakes Section (14 Cakes)
tsx
function StandardCakesSection({ cakes }) {
  return (
    <section className="container mx-auto py-16 px-4">
      <h2 className="text-4xl font-bold text-center mb-4">Standard Cakes</h2>
      <p className="text-center text-muted-foreground mb-12 max-w-2xl mx-auto">
        Choose from our signature collection of European-style cakes
      </p>
      
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
        {cakes.map((cake) => (
          <div 
            key={cake.id}
            className="bg-white rounded-lg border-2 border-gray-200 hover:border-raspberry-pink hover:shadow-lg transition p-6"
          >
            {/* Cake Icon */}
            <div className="text-5xl text-center mb-4">
              {getCakeEmoji(cake.category)}
            </div>
            
            {/* Name & Category */}
            <h3 className="text-xl font-bold text-center mb-2">
              {cake.name}
            </h3>
            <div className="text-center mb-3">
              <span className="inline-block bg-raspberry-pink/10 text-raspberry-pink px-3 py-1 rounded-full text-sm">
                {cake.category}
              </span>
            </div>
            
            {/* Description */}
            <p className="text-sm text-muted-foreground text-center mb-4">
              {cake.description}
            </p>
            
            {/* Layers Preview */}
            <div className="bg-cream p-3 rounded text-xs mb-4">
              <p className="font-semibold mb-1">Includes:</p>
              {cake.layers.map((layer, i) => (
                <p key={i}>
                  • Layer {i + 1}: {layer.flavor} / {layer.filling}
                </p>
              ))}
            </div>
            
            {/* Pricing */}
            <div className="text-center">
              <p className="text-2xl font-bold text-raspberry-pink">
                Starting at ${cake.basePrice}
              </p>
              <p className="text-xs text-muted-foreground mt-1">
                + size and customizations
              </p>
            </div>
          </div>
        ))}
      </div>
    </section>
  );
}

// Helper function
function getCakeEmoji(category) {
  const emojiMap = {
    'Classic': '🎂',
    'Premium': '🍰',
    'Fruity': '🍓',
    'Chocolate': '🍫'
  };
  return emojiMap[category] || '🎂';
}
C. Flavors, Fillings & Icings Section
tsx
function CustomizationOptionsSection({ flavors, fillings, icings }) {
  return (
    <section className="bg-cream py-16">
      <div className="container mx-auto px-4">
        <h2 className="text-4xl font-bold text-center mb-12">
          Customize Your Perfect Cake
        </h2>
        
        <div className="grid md:grid-cols-3 gap-8">
          {/* Cake Flavors */}
          <div className="bg-white rounded-lg p-6 shadow-md">
            <div className="flex items-center gap-2 mb-4">
              <span className="text-3xl">🎂</span>
              <h3 className="text-2xl font-bold">Cake Flavors</h3>
            </div>
            <ul className="space-y-2">
              {flavors.map((flavor) => (
                <li key={flavor.id} className="flex items-center gap-2">
                  <span className="text-green-600">✓</span>
                  <span>{flavor.name}</span>
                </li>
              ))}
            </ul>
          </div>

          {/* Fillings */}
          <div className="bg-white rounded-lg p-6 shadow-md">
            <div className="flex items-center gap-2 mb-4">
              <span className="text-3xl">🍫</span>
              <h3 className="text-2xl font-bold">Fillings</h3>
            </div>
            <p className="text-sm text-muted-foreground mb-3">
              Choose up to 2 per order
            </p>
            <ul className="space-y-2">
              {fillings.map((filling) => (
                <li key={filling.id} className="flex items-center gap-2">
                  <span className="text-green-600">✓</span>
                  <span className="text-sm">{filling.name}</span>
                </li>
              ))}
            </ul>
          </div>

          {/* Icing Flavors */}
          <div className="bg-white rounded-lg p-6 shadow-md">
            <div className="flex items-center gap-2 mb-4">
              <span className="text-3xl">🧁</span>
              <h3 className="text-2xl font-bold">Icing Flavors</h3>
            </div>
            <ul className="space-y-2">
              {icings.map((icing) => (
                <li key={icing.id} className="flex items-center gap-2">
                  <span className="text-green-600">✓</span>
                  <span>{icing.name}</span>
                </li>
              ))}
            </ul>
          </div>
        </div>
      </div>
    </section>
  );
}
D. Icing Colors Section (37 Colors with Swatches)
tsx
function IcingColorsSection({ colors }) {
  return (
    <section className="container mx-auto py-16 px-4">
      <h2 className="text-4xl font-bold text-center mb-4">
        Icing & Writing Colors
      </h2>
      <p className="text-center text-muted-foreground mb-12">
        Choose from {colors.length} vibrant colors
      </p>
      
      <div className="grid grid-cols-4 sm:grid-cols-6 md:grid-cols-8 lg:grid-cols-12 gap-4">
        {colors.map((color) => (
          <div key={color.id} className="text-center">
            <div 
              className="w-16 h-16 rounded-lg border-2 border-gray-300 mx-auto mb-2 shadow-sm hover:scale-110 transition"
              style={{ backgroundColor: color.hexCode }}
              title={color.name}
            />
            <p className="text-xs font-medium">{color.name}</p>
          </div>
        ))}
      </div>
    </section>
  );
}
E. Decorations Gallery
tsx
function DecorationsSection({ decorations }) {
  return (
    <section className="bg-cream py-16">
      <div className="container mx-auto px-4">
        <h2 className="text-4xl font-bold text-center mb-12">
          Decoration Options
        </h2>
        
        <div className="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-6 gap-4">
          {decorations.map((deco) => (
            <div 
              key={deco.id}
              className="bg-white rounded-lg p-4 text-center shadow-sm hover:shadow-md transition"
            >
              <div className="text-4xl mb-2">{deco.icon}</div>
              <p className="text-sm font-medium mb-1">{deco.name}</p>
              <p className="text-xs text-raspberry-pink font-semibold">
                +${deco.price}
              </p>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}
F. Contact CTA (Bottom)
tsx
function ContactCTA() {
  return (
    <section className="bg-raspberry-pink text-white py-20 text-center">
      <div className="container mx-auto px-4">
        <h2 className="text-4xl font-bold mb-4">Ready to Order Your Perfect Cake?</h2>
        <p className="text-xl mb-8">
          Call or email us to customize your dream cake today!
        </p>
        
        <div className="flex flex-col sm:flex-row gap-4 justify-center mb-8">
          <a 
            href="tel:713-555-2253"
            className="bg-white text-raspberry-pink px-8 py-4 rounded-lg font-semibold text-lg hover:bg-cream transition"
          >
            📞 (713) 555-CAKE
          </a>
          <a 
            href="mailto:orders@emilybakescakes.com"
            className="bg-white text-raspberry-pink px-8 py-4 rounded-lg font-semibold text-lg hover:bg-cream transition"
          >
            📧 orders@emilybakescakes.com
          </a>
        </div>
        
        <p className="text-sm opacity-90">
          📍 Visit our bakery in Houston, Texas | ⏰ Mon-Sat: 9AM - 6PM
        </p>
      </div>
    </section>
  );
}
Phase 4: Update Navigation Links
Files to Update:

src/components/Header.tsx (or wherever your nav is):

tsx
// Change all instances of /shop to /menu
<Link to="/menu">Menu</Link>
src/pages/Home.tsx (if it has shop links):

tsx
<Link to="/menu" className="btn-primary">
  View Our Menu
</Link>
Implementation Checklist
 Rename Shop.tsx → Menu.tsx

 Update route in App.tsx: /shop → /menu

 Build MenuHero component

 Build StandardCakesSection component (14 cakes)

 Build CustomizationOptionsSection component (flavors/fillings/icings)

 Build IcingColorsSection component (37 color swatches)

 Build DecorationsSection component (25+ decorations)

 Build ContactCTA component

 Update all navigation links sitewide

 Test responsive behavior (mobile/tablet/desktop)

 Verify no ordering functionality present

 Test color swatches render correctly

 Commit changes to GitHub
